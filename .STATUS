## Project: atlas
## Type: node-package
## Status: active
## Phase: v0.12.1 RELEASED
## Priority: 1
## Progress: 100

## Focus: research-safe sync + research registry — manuscripts/programs in the registry, `atlas doctor` contract audit, opt-in scanner marker
## Next: Promote the [Unreleased] dev tail (FW-30 fix #54 + cookbook #55) to main, or advance the ecosystem-integration spec (gated on obsidian-cli-ops RFC#35)

## Quick Context
Atlas is the project state engine - a standalone Node.js package providing
Clean Architecture-based state management for developer workflows. Designed
with ADHD-friendly features like streak tracking and celebration helpers.

## Features
- **Project Registry**: Track projects, status, metadata
- **Session Tracking**: Work sessions with duration and notes
- **Quick Capture**: Ideas, tasks, bugs with triage workflow
- **Breadcrumb Trail**: Context markers for "where was I?"
- **MCP Server**: 10 tools for Claude integration
- **Ink TUI Dashboard**: Multi-panel React Ink dashboard with real data
- **Real Data Pipeline**: AtlasContext + 4 hooks (useProjects, useActiveSession, useProjectStats, usePendingCaptures)
- **Visual Enhancements**: Focus score, sparklines, heatmap, 5 themes
- **Morning Ritual**: Guided daily planning (`atlas plan`)
- **Ecosystem Hub**: Cross-project `.STATUS` scanning (opt-in `.atlas-scan-children` umbrella marker)
- **Session Export**: iCal/JSON export for calendars
- **Research Registry**: `sync --from-status` parses research `.STATUS` (`kind`, `target`/`venue`, `tasks`); `project list --kind`; research-safe plain sync (`sync --research` alias + preserve-and-warn)
- **Doctor**: `atlas doctor` — read-only Project Settings Contract audit (`.STATUS`/`CLAUDE.md`/`.obs/sync.yml`) with `--fix`

## Test Stats
- Total: 1,905 passing (Jest, 80 suites) — green on Node 26 after the better-sqlite3 12.11.1 bump (12.5.0 capped at Node 25; the SQLite suite previously failed locally with a native-ABI mismatch)
- Ink: 277 passing (10 suites)
- Dogfood: 183 (shell test suite)

## Links
- **Docs**: https://data-wise.github.io/atlas/
- GitHub: https://github.com/Data-Wise/atlas
- Homebrew: `brew install data-wise/tap/atlas`
- npm: `npm install -g @data-wise/atlas`

## ✅ Just Completed (2026-06-26)
- **Adopted ESLint + CI lint gate** — merged PR #56 (eslint flat config + 3 real fixes) and PR #57 (CI `Lint` job gating PRs on `npm run lint`); resolves the long-dead lint script. dev CI green with the new Lint job. 135 pre-existing `no-unused-vars` warnings remain (non-blocking — eslint exits 0 on warnings) as a follow-up cleanup. Pruned 6 stale merged local branches + the ci-lint-gate worktree.
- **Released v0.12.1** (patch) — FW-30 id convergence (#49), better-sqlite3 Node-26 bump, PatternAnalyzer crash fix (#58), cookbook docs, +42 edge tests. dev→main PR + GitHub release; Homebrew + docs auto-deploy.
- **Merged PR #58** (feature/test-hardening → dev, squash `40fb761`): 42 edge tests across the v0.11–v0.12 research-sync surface + v0.10 temporal intelligence, **plus a real PatternAnalyzer crash fix** — an unparseable string `startTime` threw a TypeError that crashed the whole `analyze()` call (`byDay[undefined].total++`). Also confirmed the scanner `>`/`>=` depth guards are compensating conventions, not a bug. Suite 1947/1947 (89 suites), CI green on Node 18/20/22.
- **Fixed Node 26 test failures** — bumped better-sqlite3 `^12.5.0` → `^12.11.1` (12.5.0 capped at Node 25; V8 removed `PropertyCallbackInfo::This()`). 29 SQLite tests now green locally.
- **Released v0.12.0** — `atlas sync --research` alias (defaults to `~/projects/research`); plain-sync research **warning** that names projects it preserved-but-did-not-refresh, with the remedy; `.atlas-scan-children` opt-in marker so an umbrella dir's child repos get scanned (bounded by maxDepth); venue/`target:` inline-comment strip (`CSDA # was JASA` → `CSDA`); focused unit tests for the research `list()` surface (#40/#41/#42/#44)
- **Released v0.11.1** — plain `atlas sync` / `--remove-orphans` no longer strips research metadata; `kind`/`target`/`tasks`/`priorityLabel` carried forward on update via `_preserveResearchMetadata`; regression test (#36)
- On **dev, awaiting next release** (`[Unreleased]` tail, 4 ahead of main): FW-30 sync id-convergence — plain sync resolves an existing project by **path** so it updates the `sync --from-status` entry instead of duplicating (#54/#49); research-ops **Cookbook** (10 recipes) + API-GUIDE Doctor API + tutorial/ARCHITECTURE refresh (#55)

## ✅ Just Completed (2026-06-25)
- **Released v0.11.0** — **research registry**: `sync --from-status` parses research `.STATUS` (`kind:` manuscript|program, `target:`/`venue:`, `tasks:` block), `summarize()` groups `byKind`, `project list --kind` filter, `kind`/`target`/`taskCount` in JSON + MCP; **`atlas doctor`** read-only contract audit (`--kind`/`--all-registered`/`--fix`); `project list --json` adds `progress`/`next`/`priority`; registry-load robustness (description truncation stops one corrupt row bricking findAll) + `ATLAS_DATA_DIR` env-precedence fix

## ✅ Just Completed (2026-06-20)
- Wrote **SPEC-ecosystem-integration-gaps** (docs/specs/, committed `a92f4e3`) — cross-repo map for atlas↔nexus + flow-cli↔obsidian-cli-ops; 7 locked decisions; RFC#35-verified; corrected a stale "atlas↔nexus done" claim (real state = a 5-month-stale text-parsing MCP shim in `statistical-research`)
- Verified v0.10.0 release integrity — "dev N ahead of main" is a benign **squash-merge artifact** (zero code diff vs main), not an unreleased gap; project memory updated
- Cross-repo (research-domain): built the **MediationVerse dashboard sync** as a local launchd job (replacing a documented-but-nonexistent sync); fixed 3 false auto-sync claims; regenerated dashboard from `.STATUS`; restructured medfit/medsim `.STATUS` (next:/done: convention). Script committed in new `r-packages/scripts` repo

## ✅ Just Completed (2026-06-19)
- **Released v0.10.0** — PR #19 (feature→dev) + PR #20 (dev→main) merged; tag v0.10.0 + GitHub release published; Homebrew auto-update triggered
- Temporal intelligence: VelocityCalculator, PatternAnalyzer, PredictionEngine; `atlas stats --velocity / --patterns / --calibrate`; 31 unit tests
- Fixed dogfood version assertion (0.9.x → 0.10.x) before release merge

## ✅ Previously Completed (2026-06-14)
- Merged PR #17 → dev: flow-cli integration flags F1–F5 (session status --format json, project list --count/--suggest, inbox --count, trail --limit) + status-filter fix
- Reviewed PR #17, applied 3 recommendations (deterministic F1 test, NaN guards, help precedence)
- Added docs CI strict-build gate (docs.yml validate job); fixed 4 pre-existing broken spec links
- Wrote SPEC-flow-cli-integration (atlas) + SPEC-atlas-surface-standard (flow-cli, on flow-cli dev)
- Reviewed + corrected + merged PR #16 (F6 inbox-type-filter spec) → dev — fixed fabricated SQLite migration claim, gated back-fill on flow-cli verification
- **Released v0.9.3** — PR #18 (dev→main) merged, tag v0.9.3, GitHub release, Homebrew formula + docs deploy all green

## ✅ Previously Completed (2026-02-23)
- Released v0.9.2 — Real Data Pipeline
- Reviewed PR #13: caught auto-select bug + ORCHESTRATE cleanup
- Merged feature/ink-real-data to dev, cleaned up worktree
- Updated 6 docs (index, API-GUIDE, CLI-REFERENCE, ROADMAP, REFCARD, TUTORIAL)
- Fixed stale dogfood test (MOCK_ constants → hooks assertions)
- Full release pipeline: version bump, PR #14, merge, GitHub release, Homebrew, docs deploy
- Updated project memory with PR review patterns and doc update scope

## ✅ Previously Completed (2026-02-22)
- Released v0.9.1 to GitHub, Homebrew, docs site
- Designed and orchestrated Ink real data wiring feature

## 🔴 Blockers
- None

## 🔧 Active Worktrees
- (none)

## 🎯 Next Action
A) **Promote the [Unreleased] dev tail to main** — FW-30 id-convergence fix (#54/#49) + research-ops cookbook (#55) are on dev only (4 ahead of main). Cut a v0.12.1/docs release if wanted; otherwise they ride the next feature release. The gap is benign pre-release accumulation, not a missed release.
B) **Advance the ecosystem-integration spec** (docs/specs/SPEC-ecosystem-integration-gaps-2026-06-20.md) — still the real forward work; gated on obsidian-cli-ops **RFC#35** (`obs write`). One step buildable now: flow-cli `obs-bridge` (read-only). atlas's own part (P1: `catch` → obs write-through) waits on RFC#35.
C) Plan v0.13.0 — deferred v0.10 items (AnalyticsView in Ink, dead-zone iCal, multi-project calibration) or platform expansion (remote sync, IDE extensions)
